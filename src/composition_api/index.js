import cookie from './cookie';
import dateFormat from './dateFormat';
import { lastDeduct } from './lastDeduct';

const Cookie = new cookie();
const DateFormat = new dateFormat();

export { Cookie, DateFormat, lastDeduct }